name: Deploy to Cloudflare Workers

on:
  push:
    branches:
      - master

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Sort blog, sitemap, optional R2/CDN asset base
        env:
          # Repository variable: public HTTPS origin for R2 (or any CDN) mirroring `assets/` paths.
          RH_ASSETS_BASE_URL: ${{ vars.RH_ASSETS_BASE_URL }}
        run: npm ci && npm run sort-blog && npm run sitemap && npm run apply-assets-cdn

      - name: Deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
